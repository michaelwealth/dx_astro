---
import Layout from '../layouts/Layout.astro';
import ContactForm from '../components/ContactForm';
---

<Layout 
  title="Contact Us" 
  description="Get in touch with Digital Xceeded. Let's discuss how our integrated approach can help you achieve your business goals."
  hideFooterCta={true}
>
  <!-- Page Header -->
  <section class="page-header">
    <div class="container">
      <div class="header-content">
        <span class="page-label">Contact</span>
        <h1 class="page-title">Let's build something <span class="text-gradient">extraordinary</span></h1>
        <p class="page-subtitle">
          Every transformative project starts with a conversation. Tell us about your vision, 
          and let's explore how our integrated approach can bring it to life.
        </p>
      </div>
    </div>
    <div class="header-decoration" aria-hidden="true">
      <div class="decoration-circle"></div>
      <div class="decoration-circle"></div>
    </div>
  </section>
  
  <ContactForm client:visible />
  
  <!-- Additional Info -->
  <section class="additional-info">
    <div class="container">
      <div class="info-grid">
        <div class="info-card">
          <div class="info-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"/>
              <path d="M12 6v6l4 2"/>
            </svg>
          </div>
          <h3>Quick Response</h3>
          <p>We respond to all inquiries within 24 hours. For urgent matters, give us a call.</p>
        </div>
        
        <div class="info-card">
          <div class="info-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
              <circle cx="9" cy="7" r="4"/>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
              <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
            </svg>
          </div>
          <h3>Dedicated Team</h3>
          <p>You'll work with senior strategists and specialists who understand your industry.</p>
        </div>
        
        <div class="info-card">
          <div class="info-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
              <polyline points="22,4 12,14.01 9,11.01"/>
            </svg>
          </div>
          <h3>No Obligation</h3>
          <p>Our initial consultation is free. Let's explore if we're the right fit for your project.</p>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .page-header {
    position: relative;
    padding: calc(var(--header-height) + var(--space-4xl)) 0 var(--space-3xl);
    background: linear-gradient(180deg, var(--color-white) 0%, var(--color-cream) 100%);
    overflow: hidden;
  }
  
  .header-content {
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }
  
  .page-label {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-primary);
    margin-bottom: var(--space-md);
  }
  
  .page-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 800;
    margin-bottom: var(--space-lg);
  }
  
  .page-subtitle {
    font-size: 1.25rem;
    color: var(--color-text-light);
    line-height: 1.7;
  }
  
  .header-decoration {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }
  
  .decoration-circle {
    position: absolute;
    border-radius: 50%;
    border: 1px solid rgba(255, 87, 87, 0.1);
  }
  
  .decoration-circle:nth-child(1) {
    width: 600px;
    height: 600px;
    top: -300px;
    right: -200px;
  }
  
  .decoration-circle:nth-child(2) {
    width: 400px;
    height: 400px;
    bottom: -200px;
    left: -100px;
    border-color: rgba(255, 152, 0, 0.1);
  }
  
  .additional-info {
    padding: var(--space-4xl) 0 var(--space-5xl);
    background: var(--color-white);
  }
  
  .info-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-xl);
  }
  
  .info-card {
    text-align: center;
    padding: var(--space-2xl);
  }
  
  .info-icon {
    width: 64px;
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--gradient-warm-soft);
    border-radius: var(--radius-xl);
    color: var(--color-primary);
    margin: 0 auto var(--space-lg);
  }
  
  .info-card h3 {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: var(--space-md);
  }
  
  .info-card p {
    color: var(--color-text-light);
    line-height: 1.6;
  }
  
  @media (max-width: 768px) {
    .info-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
